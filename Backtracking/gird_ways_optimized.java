package Backtracking;

public class gird_ways_optimized {
      // trick to find the number of ways by using permutations
        // simple formula to find when it has n rows and m columns is
        // ways = (((n-1) + (m-1))!)/((n-1)!*(m-1)!)
}